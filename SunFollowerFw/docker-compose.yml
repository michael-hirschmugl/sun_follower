version: '3.8'

services:
  build-env:
    build:
      context: .
      dockerfile: Dockerfile
    image: sunfollower-build:latest
    container_name: sunfollower-build
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
